# -----------------------------------
# Rosetta stone of Cython and Blaze
# -----------------------------------

int is int

double is double

float is float

long is long

char is char

uint is uint

array(T, x) is (x, T)

pointer is Ptr

union is Union

struct is Record
